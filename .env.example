# ถ้าตั้งค่านี้ ระบบจะดึงข้อมูลจาก Cloud HTTP Source
# ตัวอย่าง: https://your-cloud.example.com (ต้องมี /telemetry รองรับ ?limit=)
CLOUD_BASE_URL=

# (ถ้ามี) ใส่ API key เพื่อส่งเป็น Authorization: Bearer <token>
CLOUD_API_KEY=
